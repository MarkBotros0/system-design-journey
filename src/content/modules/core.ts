import type { Module } from '../types'

/**
 * Track 2 — Core concepts.
 *
 * The framework, and the trade-offs every design turns on. Authored after the app shell
 * was verified end to end against Foundations.
 */
export const coreModules: Module[] = []
